//import { PELDA_ACTION } from '../constants/actionTypes';

const INITIAL_STATE = {
  pelda: false,
};

const peldaReducer2 = (state = INITIAL_STATE, action) => {
    return {
     INITIAL_STATE
  }
};

export default peldaReducer2;
